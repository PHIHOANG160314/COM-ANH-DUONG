# CC CLI TASK: FINAL BATTLE 10x Bootstrap

## 🔥 CÔNG THÀNH PHÁ TRẬN - Total Victory

**Mode**: Deep audit + aggressive fix
**Time**: Single session mega cleanup
**Goal**: Production-ready 100%

---

## 🔴 FRONT 1: Tech Debt Elimination

```bash
# Find all tech debt
grep -rn "TODO\|FIXME\|XXX\|HACK" src --include="*.ts" --include="*.tsx"
grep -rn "console\.\(log\|warn\|error\)" src --include="*.ts" --include="*.tsx"
grep -rn "@ts-ignore\|@ts-nocheck" src --include="*.ts" --include="*.tsx"
```

**Actions:**

- Replace console.\* with Debug utility from `shared/lib/debug.ts`
- Remove or fix all TODO comments
- Remove all @ts-ignore

---

## 🔴 FRONT 2: Type Safety 100%

```bash
# Find any types
grep -rn ": any\|as any\|<any>" src --include="*.ts" --include="*.tsx"
```

**Actions:**

- Replace `any` with proper types
- Add interfaces where missing
- Use generics where appropriate

---

## 🟡 FRONT 3: Performance Optimization

```bash
# Check bundle size
npm run build 2>&1 | grep -E "chunk|size"
```

**Actions:**

- Add lazy loading to routes (React.lazy)
- Configure manualChunks in vite.config.ts
- Optimize images to webp if not done

---

## 🟡 FRONT 4: Security Hardening

**Actions:**

- Verify all inputs are validated (zod schemas)
- Check for XSS in any dangerouslySetInnerHTML
- Verify env vars not exposed

---

## 🟢 FRONT 5: UX Polish

**Actions:**

- Add loading states to all async operations
- Add error boundaries around feature modules
- Improve empty states with illustrations

---

## 🟢 FRONT 6: Documentation

**Actions:**

- Update README with deployment guide
- Add inline code comments for complex logic
- Update stakeholder-sops.md with new features

---

## Success Criteria

- [ ] grep -r "console\." returns 0 (except Debug)
- [ ] grep -r "TODO\|FIXME" returns 0
- [ ] grep -r ": any" returns 0
- [ ] npm run build passes clean
- [ ] npm run lint passes (if exists)

## Commit Convention

```
refactor: 10x bootstrap - total codebase cleanup for production
```

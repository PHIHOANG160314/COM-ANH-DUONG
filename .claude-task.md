# CC CLI TASK: COD User Journey Optimization

## Context

Site: comanhduong.com - Restaurant POS
Focus: Seamless Cash on Delivery (COD) checkout experience
Launch: Ready for customer soft launch

---

## User Journey Map (COD Focus)

```
Home → Menu → Add to Cart → Checkout → COD Selected → Đặt Hàng → Order Success → Kitchen KDS → Delivery → COD Collection
```

---

## 🔴 Priority 1: Highlight COD as Recommended

File: `react-app/src/features/payment/components/payment-method-selector.tsx`

**Action**: Add "Phổ biến" badge to COD option:

```tsx
<Chip label="Phổ biến" size="small" color="success" />
```

Make COD visually prominent (green border, emoji 💵).

---

## 🔴 Priority 2: Polish Order Success Page

File: `react-app/src/pages/customer/order-success-page.tsx`

**Check & Improve**:

- Show clear order number
- Add "Thời gian dự kiến: 30-45 phút"
- Show COD amount to prepare
- Add WhatsApp/Zalo contact button

---

## 🔴 Priority 3: Clean Console.error

File: `react-app/src/pages/customer/checkout-page.tsx`

Line 182: `console.error('Checkout error:', err);`

**Action**: Replace with Debug.error or remove for production.

---

## 🟡 Priority 4: Add Cart to Menu Flow CTA

Ensure clear "Thêm vào giỏ" → "Xem giỏ hàng" → "Thanh toán" flow.

Check `menu-showcase.tsx` and `product-card.tsx` have visible CTAs.

---

## 🟡 Priority 5: Mobile UX Check

```bash
# Check viewport meta
grep -r "viewport" react-app/index.html

# Check responsive styles
grep -rn "@media" react-app/src/ --include="*.css" --include="*.tsx" | head -20
```

---

## 🟢 Priority 6: Update User Journey SOPs

Update `react-app/docs/stakeholder-sops.md` with COD-focused customer journey:

```markdown
## Customer Journey (COD)

1. Browse menu → Add items to cart
2. View cart → Proceed to checkout
3. Fill delivery info (auto-fill if logged in)
4. Select COD (default, highlighted)
5. Click "Đặt hàng"
6. Receive order confirmation
7. Prepare cash for delivery
8. Receive order + pay
```

---

## Success Criteria

- [ ] COD has "Phổ biến" badge
- [ ] Order success shows COD amount to prepare
- [ ] No console.error in production
- [ ] Mobile checkout smooth
- [ ] SOPs updated with COD journey

## Commit Convention

```
feat: optimize COD user journey for seamless checkout
```

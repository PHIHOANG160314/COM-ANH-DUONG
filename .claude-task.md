# CC CLI GO-LIVE FINAL PUSH

## 🚀 MISSION: GO-LIVE TRONG 2 GIỜ

**Priority**: CRITICAL - Ship to production
**Target**: 100% GO-LIVE Ready

---

# PHASE 5: PERFORMANCE OPTIMIZATION (CRITICAL)

## 5.1 Bundle Size (Current: 540KB > Target: 500KB)

### Code Splitting với React.lazy

```tsx
// router.tsx - Lazy load all route components
const CustomerHomePage = lazy(() => import("@/pages/customer/home-page"));
const AdminDashboard = lazy(() => import("@/pages/admin/admin-dashboard-page"));
// ... all other pages
```

### Manual Chunks trong vite.config.ts

```js
build: {
  rollupOptions: {
    output: {
      manualChunks: {
        'vendor-react': ['react', 'react-dom', 'react-router-dom'],
        'vendor-mui': ['@mui/material', '@mui/icons-material'],
        'vendor-motion': ['framer-motion'],
        'vendor-supabase': ['@supabase/supabase-js'],
      }
    }
  }
}
```

**Target**: Bundle < 500KB gzipped

---

# PHASE 6: SECURITY HARDENING

## 6.1 Input Validation với Zod

### Install

```bash
npm install zod
```

### Checkout Schema

```ts
// shared/lib/schemas.ts
import { z } from "zod";

export const checkoutSchema = z.object({
  customerName: z.string().min(2, "Tên phải có ít nhất 2 ký tự"),
  phone: z.string().regex(/^0\d{9}$/, "Số điện thoại không hợp lệ"),
  address: z.string().min(10, "Địa chỉ quá ngắn"),
  paymentMethod: z.enum(["cod", "bank", "momo"]),
});
```

### Apply to checkout-page.tsx

---

# PHASE 8: GO-LIVE VALIDATION

## Functional Checklist

### Customer Flow

- [ ] Homepage loads < 3s
- [ ] Hero section animations smooth
- [ ] Menu displays all products
- [ ] Add to cart works with animations
- [ ] Cart drawer shows items correctly
- [ ] Checkout form validates
- [ ] COD payment completes
- [ ] Order success page shows

### Admin Flow

- [ ] Login works
- [ ] Dashboard shows stats
- [ ] Orders list loads
- [ ] Can update order status

### Kitchen/Staff Flow

- [ ] Kitchen display shows orders
- [ ] POS accessible
- [ ] Delivery page works

### Technical

- [ ] npm run build passes
- [ ] No console.error in production
- [ ] PWA installable
- [ ] Mobile responsive

---

# COMMIT CONVENTION

```bash
# After each phase
git add -A && git commit -m "feat: Phase 5 - Performance optimization"
git add -A && git commit -m "feat: Phase 6 - Security hardening"
git add -A && git commit -m "feat: GO-LIVE ready - production deployment"
```

---

# SUCCESS CRITERIA

```bash
# Final validation
npm run build          # Must pass
# Bundle < 500KB gzip  # Check output
grep -r "console." src | wc -l  # Should be 0
```

## GO-LIVE Definition

1. ✅ Build passes
2. ✅ Bundle < 500KB
3. ✅ Zod validation on checkout
4. ✅ All pages accessible
5. ✅ Mobile responsive
6. ✅ PWA installable

---

# EXECUTION ORDER

1. **Immediate** (30 min):
   - Code splitting với React.lazy
   - Configure manualChunks
2. **Next** (30 min):
   - Install Zod
   - Add checkout validation
3. **Final** (30 min):
   - Run all validations
   - Fix any issues
   - Push to production

**DO NOT STOP UNTIL GO-LIVE COMPLETE!** 🚀

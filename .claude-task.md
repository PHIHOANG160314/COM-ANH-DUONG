# CC CLI FULL-STACK BOOTSTRAP TASK

## 🚀 MISSION: Xây dựng lại từ đầu - MAX LEVEL WOW

**Priority**: CRITICAL
**Target**: Production-ready Vietnamese F&B Web App

---

# PHASE 1: UI/UX AUDIT (15 min)

## Check ALL pages for broken UI:

1. Homepage (`/`) - Hero, Menu, Info cards
2. Menu page (`/menu`) - Products grid
3. Checkout (`/checkout`) - Form, cart summary
4. Order success (`/order-success`)
5. Admin dashboard (`/admin`)
6. Kitchen display (`/kitchen`)
7. POS (`/pos`)
8. Delivery (`/delivery`)

## Fix common issues:

- Text không hiển thị (check dark theme colors)
- Images không load
- Responsive mobile
- Font Inter loading

---

# PHASE 2: DARK THEME FIX (20 min)

## Problem: Text invisible on dark background

## Solution: Use explicit colors, not theme tokens

### Files to check:

- `src/features/home/components/hero-section.tsx`
- `src/features/cart/components/cart-drawer.tsx`
- `src/features/menu/components/menu-grid.tsx`
- `src/shared/layouts/main-layout.tsx`

### Color fixes:

```tsx
// BAD (invisible on dark theme)
color="text.primary"
color="text.secondary"

// GOOD (explicit colors)
sx={{ color: '#1a1a2e' }}
sx={{ color: '#666' }}
sx={{ color: 'white' }}
```

---

# PHASE 3: MOBILE RESPONSIVE (20 min)

## Check breakpoints:

- Mobile: 375px
- Tablet: 768px
- Desktop: 1024px+

## Components to verify:

- Navigation hamburger menu
- Cart drawer mobile width
- Product cards stacking
- Checkout form padding
- Button sizes (min 44px touch target)

---

# PHASE 4: PWA ENHANCEMENTS (15 min)

## PWA Install Prompt:

Add custom install button in header/footer

```tsx
import { useState, useEffect } from "react";

export const useInstallPrompt = () => {
  const [installPrompt, setInstallPrompt] = useState<any>(null);

  useEffect(() => {
    const handler = (e: Event) => {
      e.preventDefault();
      setInstallPrompt(e);
    };
    window.addEventListener("beforeinstallprompt", handler);
    return () => window.removeEventListener("beforeinstallprompt", handler);
  }, []);

  return installPrompt;
};
```

## Manifest check:

- Icons: /pwa-64x64.png, /pwa-192x192.png, /pwa-512x512.png
- Name: "Cơm Ánh Dương"
- Display: standalone
- Theme color: #4ade80

---

# PHASE 5: IMAGES & ASSETS (15 min)

## Menu images:

Verify all images exist in `/public/images/menu/`:

- com_suon_nuong.png
- com_ga_xoi_mo.png
- pho_bo_tai.png
- bun_bo_hue.png
- banh_mi_dac_biet.png
- com_tam_bi_cha.png
- bac_xiu.png
- ca_phe_sua_da.png
- sua_chua_danh_da.png

## Image optimization:

- Convert to WebP if possible
- Add lazy loading
- Add blur placeholder

---

# PHASE 6: FINAL VALIDATION (15 min)

## Build check:

```bash
npm run build
# Must pass with no errors
```

## Console check:

```bash
# No console.error in production
grep -r "console\." src --include="*.tsx" | grep -v debug
```

## TypeScript check:

```bash
npm run type-check
# Zero errors
```

## Commit:

```bash
git add -A
git commit -m "feat: full-stack bootstrap complete - production ready"
git push origin main
```

---

# SUCCESS CRITERIA

| Metric     | Target               |
| ---------- | -------------------- |
| Build      | ✅ Pass              |
| Bundle     | < 150KB gzipped main |
| TypeScript | 0 errors             |
| Console    | 0 errors             |
| Mobile     | 100% responsive      |
| PWA        | Installable          |
| Dark Theme | All text visible     |

---

# EXECUTION ORDER

1. **UI Audit** → Fix broken components
2. **Dark Theme** → Explicit colors everywhere
3. **Mobile** → Responsive all pages
4. **PWA** → Install prompt
5. **Images** → Verify all load
6. **Build & Push** → Deploy to Vercel

**DO NOT STOP UNTIL ALL 6 PHASES COMPLETE!** 🚀

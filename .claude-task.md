# CC CLI BOOTSTRAP TASK: SOPs F&B CHUẨN ĐÔNG NAM Á

## 🎯 MISSION: Chuyển đổi kiến trúc COM-ANH-DUONG thành SOPs F&B chuẩn Đông Nam Á

**Commander**: Opus Antigravity
**Mode**: FULL COMMAND CONTROL - AUTO EXECUTE
**Target**: PRODUCTION GO-LIVE

---

## BINH PHÁP 6 MẶT TRẬN

### 🔴 Front 1: TECH DEBT ELIMINATION (始計)

```bash
# Thực thi ngay
grep -r "console\." react-app/src --include="*.tsx" --include="*.ts" | grep -v debug
grep -r "TODO\|FIXME" react-app/src | head -20
grep -r "@ts-ignore" react-app/src | head -10
```

**Target**: 0 items

### 🔴 Front 2: TYPE SAFETY 100% (作戰)

```bash
grep -r ": any" react-app/src --include="*.tsx" --include="*.ts" | head -20
```

**Target**: 0 `any` types

### 🟡 Front 3: PERFORMANCE (謀攻)

- ✅ React.lazy code splitting DONE
- ✅ Vendor chunks DONE
- [ ] Image WebP optimization
      **Target**: Build < 10s

### 🟡 Front 4: SECURITY (軍形)

```bash
# Install zod if not exists
npm list zod || npm install zod
```

- [ ] Input validation schemas
- [ ] XSS prevention
      **Target**: 0 vulnerabilities

### 🟢 Front 5: UX POLISH (兵勢)

- [ ] Loading states everywhere
- [ ] Error boundaries
- [ ] Empty states
      **Target**: Seamless UX

### 🟢 Front 6: SOPs F&B ĐÔNG NAM Á (虛實)

- ✅ Địa chỉ: Phường Sa Đéc, Tỉnh Đồng Tháp
- ✅ VND currency formatting
- ✅ Vietnamese UI text
- [ ] COD prominence
- [ ] Zalo/WhatsApp contact widgets
- [ ] Operating hours (6:00-21:00)
      **Target**: SEA F&B Standard

---

## EXECUTION ORDER (AUTO RUN)

### Step 1: Audit Tech Debt

```bash
cd /Users/macbookprom1/mekong-cli/com-anh-duong-10x/react-app
echo "=== CONSOLE STATEMENTS ===" && grep -r "console\." src --include="*.tsx" --include="*.ts" | grep -v debug | wc -l
echo "=== TODO/FIXME ===" && grep -r "TODO\|FIXME" src | wc -l
echo "=== ANY TYPES ===" && grep -r ": any" src | wc -l
echo "=== TS-IGNORE ===" && grep -r "@ts-ignore" src | wc -l
```

### Step 2: Fix All Issues Found

For each issue category:

- Open file
- Fix issue
- Commit with: `refactor: 10x bootstrap - [specific fix]`

### Step 3: Build Verification

```bash
npm run build
# Must pass with no errors
# Bundle < 500KB gzipped
```

### Step 4: Commit & Push

```bash
git add -A
git commit -m "feat: SOPs F&B Đông Nam Á - production ready"
git push origin main
```

---

## VICTORY CRITERIA

| Metric     | Target       | Verify                               |
| ---------- | ------------ | ------------------------------------ |
| console.\* | 0            | `grep -r "console\." src \| wc -l`   |
| TODO/FIXME | 0            | `grep -r "TODO\|FIXME" src \| wc -l` |
| any types  | 0            | `grep -r ": any" src \| wc -l`       |
| Build      | PASS         | `npm run build`                      |
| Bundle     | < 150KB main | Check output                         |

---

## AUTO-APPROVE COMMANDS

```bash
# All these commands are pre-approved for auto-run:
grep, cat, ls, find, wc, head, tail
npm run build, npm run lint, npm test
git status, git add, git commit, git push
```

**EXECUTE ALL STEPS WITHOUT STOPPING UNTIL GO-LIVE!** 🚀

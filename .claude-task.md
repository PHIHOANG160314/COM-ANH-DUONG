# 🎯 Mission: UI/UX Binh Pháp Phase 3 - 3 Tasks

## Context

Project: Cơm Ánh Dương - SEA F&B WOW App
Path: `/Users/macbookprom1/mekong-cli/com-anh-duong-10x/react-app`
Tech: React + MUI v6 + TypeScript

---

## 🔴 Task 1: Cart Sheet (Bottom Drawer)

Create a gesture-ready bottom sheet for the shopping cart.

### File: `src/features/cart/components/cart-sheet.tsx`

Requirements:

- Use MUI `Drawer` with `anchor="bottom"`
- Rounded corners: `borderTopLeftRadius: 16, borderTopRightRadius: 16`
- Max height: `90vh`
- Sticky checkout button at bottom with `position: sticky`
- Display cart items with quantity controls (+/-)
- Show total amount using `formatCurrency`
- Dark mode compatible (use theme tokens only)
- Props: `open: boolean`, `onClose: () => void`

### File: `src/features/cart/hooks/use-cart-sheet.ts`

```typescript
// Simple hook for sheet state
export const useCartSheet = () => {
  const [isOpen, setIsOpen] = useState(false);
  const open = () => setIsOpen(true);
  const close = () => setIsOpen(false);
  return { isOpen, open, close };
};
```

### Integration

- Export from `src/features/cart/index.ts`
- Do NOT modify main-layout yet

---

## 🔴 Task 2: Pull-to-Refresh on Menu Page

Implement native pull-to-refresh for menu data sync.

### File: `src/features/menu/hooks/use-pull-to-refresh.ts`

Requirements:

- Pull threshold: 80px
- Touch gesture handling: `touchstart`, `touchmove`, `touchend`
- Return: `{ isRefreshing, pullDistance, handlers }`
- Visual feedback via pull distance

### File: `src/features/menu/components/pull-to-refresh-wrapper.tsx`

Requirements:

- Wrap children with touch handlers
- Show `CircularProgress` when pulling
- Opacity scales with pull distance
- Call `onRefresh` callback when threshold met
- Only enable on mobile (xs breakpoint via `useMediaQuery`)

### Integration

- Export from `src/features/menu/index.ts`
- Do NOT modify menu-showcase yet (separate task)

---

## 🟡 Task 3: Skeleton Loading States

Eliminate blank screen with skeleton placeholders.

### File: `src/features/menu/components/product-card-skeleton.tsx`

Requirements:

- Match exact dimensions of `ProductCard` in menu
- Image skeleton: 140px height, rectangular
- Title skeleton: 60% width
- Price skeleton: 40% width
- Use MUI `Skeleton variant="rectangular" animation="wave"`
- Theme-aware colors

### File: `src/features/menu/components/menu-skeleton.tsx`

Requirements:

- 6-item grid layout (2 columns on mobile)
- Uses `ProductCardSkeleton` component
- Category chips skeleton row (4 chips, 60px width each)
- Match `menu-showcase.tsx` grid layout

### Integration

- Export from `src/features/menu/index.ts`

---

## Acceptance Criteria

1. ☑️ All files use MUI v6 theme tokens (NO hardcoded colors)
2. ☑️ All files < 200 lines
3. ☑️ TypeScript strict mode compatible
4. ☑️ Dark mode works (test with theme toggle)
5. ☑️ Build passes: `npm run build`
6. ☑️ Commit format: `feat(ui): [component]`

---

## Execution Commands

```bash
# After completing all tasks
cd /Users/macbookprom1/mekong-cli/com-anh-duong-10x/react-app
npm run build

# If build passes
cd ..
git add -A
git commit -m "feat(ui): Phase 3 - cart sheet, pull-to-refresh, skeleton loading"
git push origin main
```

---

_Mission Generated: 2026-02-03 22:38 | Binh Pháp Phase 3_

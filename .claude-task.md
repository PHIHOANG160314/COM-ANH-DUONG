# CC CLI Task: Fix Unit Tests for CI/CD (Based on cd03472)

## Context

- Current commit: `cd03472` - "[Binh Pháp] Final Mile: 100/100 Diamond Standard"
- CI/CD GitHub Actions đang FAIL do 3 unit tests
- Bản `cd03472` là bản CHUẨN - KHÔNG được thay đổi logic business

## Problem

3 unit tests failing:

### 1. `src/shared/utils/store-hours.test.ts`

```
FAIL: should have correct configuration from CONTACT_INFO
Expected: openHour = 8
Received: openHour = 6
```

### 2. `src/shared/ui/operating-hours.test.tsx`

```
FAIL: returns closing status when 30 mins before close
Expected: "closing"
Received: "closed"

FAIL: returns closed status when before open hours
Expected: "closed"
Received: "open"
```

## Task

1. **KHÔNG thay đổi business logic** trong production code
2. **Sửa tests** để match với thực tế của bản cd03472
3. Check giá trị thật của `STORE_CONFIG` và `CONTACT_INFO`
4. Update các expect values trong tests cho khớp
5. Run `npm test` để verify tất cả 86 tests pass
6. Commit với message: `fix(tests): align unit tests with cd03472 baseline`
7. Push để CI/CD pass

## Commands

```bash
cd react-app
npm test
```

## Success Criteria

- [ ] All 86 tests pass
- [ ] CI/CD GitHub Actions pass (green check)
- [ ] KHÔNG thay đổi production code, chỉ thay đổi test files

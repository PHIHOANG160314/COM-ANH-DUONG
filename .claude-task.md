# 🎯 MISSION: GREEN GO-LIVE F&B SOP COMPLETION

## CRITICAL BUGS TO FIX (NO SKIP)

### 1. 🔴 Menu Images Not Loading (Homepage)

**Problem**: 58 món không load được hình - chỉ hiện màu xám
**File**: `react-app/src/features/menu/components/menu-grid.tsx` hoặc menu-card
**Solution**:

- Lazy load images với placeholder
- Add `loading="lazy"` to all images
- Implement skeleton loading
- Check image_url từ Supabase

### 2. 🔴 Mobile Footer Overlap

**Problem**: 3 buttons (Gọi Ngay, Zalo, Đặt Hàng) đè lên content
**Files**: Look for floating-cta, mobile-footer, or bottom-bar components
**Solution**:

- Add `padding-bottom: 80px` to main content khi có footer
- Ensure z-index proper
- Test on 375px viewport

---

## F&B SOP FEATURES TO COMPLETE

### 3. 🟡 Saved Items (Favorites)

**Database**: `saved_items` table EXISTS
**Missing**:

- ❤️ Heart button on menu cards
- API hook `use-favorites.ts`
- Favorites list in ProfilePage

### 4. 🟡 Stock/Inventory Display

**Database**: `stock_quantity` column in menu_items EXISTS
**Missing**:

- "Còn X phần" badge on cards when stock_quantity < 10
- "Hết hàng" overlay when stock_quantity = 0
- Button disable when sold out

### 5. 🟡 Daily Menu Config Realtime

**Database**: `daily_menu_config` table EXISTS
**Missing**:

- Subscribe to realtime changes
- Auto-refresh across devices

---

## PERFORMANCE OPTIMIZATION

### 6. Refactor for Performance

- Implement React.memo on MenuCard component
- Add virtualization for 58+ items list (react-window)
- Image lazy loading with IntersectionObserver
- Skeleton loading states

### 7. Security Audit

Run these checks:

- No exposed API keys in client code
- RLS policies on all tables
- CORS properly configured
- No XSS vulnerabilities in user inputs

---

## EXECUTION RULES

1. **FIX BUG #1 AND #2 FIRST** - These are blocking
2. Work sequentially, commit after each feature
3. Run `npm run build` to verify after each change
4. NO QUESTIONS - just implement best practice
5. Push after each commit: `git push origin main`

## SUCCESS CRITERIA

- [ ] All 58 menu images load with lazy loading
- [ ] Mobile footer doesn't overlap content
- [ ] ❤️ Favorites button on menu cards
- [ ] Stock quantity badge visible
- [ ] Build passes with 0 errors
- [ ] All commits pushed to main

---

**Binh Pháp**: 火攻 (Fire Attack) - Fast, decisive, no hesitation
**Target**: GREEN GO-LIVE by end of session

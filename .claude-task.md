# 🎨 Mission: Bootstrap Pencil MCP & Execute Design Workflow

## Context

Đây là dự án Cơm Ánh Dương - SEA F&B React App with MUI v6.
Project: `/Users/macbookprom1/mekong-cli/com-anh-duong-10x/react-app`

---

## Phase 1: Enable Pencil MCP Server (Priority: HIGH)

### Step 1.1: Check available MCP servers

```bash
claude mcp list
```

### Step 1.2: Enable Pencil MCP if not already

```bash
# Check if pencil-mcp is in settings
cat ~/.claude.json | jq '.mcpServers'

# If not present, add pencil-mcp server
claude mcp add pencil --server-path /path/to/pencil-mcp
```

### Step 1.3: Verify MCP connection

```bash
claude mcp status pencil
```

---

## Phase 2: UI/UX Design Tasks

### Task 2.1: Quick Mockup - Dark Mode Food Card

**Mode**: `/design:fast`
**Target**: New food card component with dark mode support

Requirements:

- Dark theme compatible (use MUI theme tokens)
- Price display with Vietnamese đồng format
- Product image with fallback
- Add-to-cart button with hover animation
- 44px minimum touch target
- Responsive: mobile-first

Output files:

- `react-app/src/shared/ui/food-card-v2.tsx`
- Update theme tokens if needed

### Task 2.2: Production-Ready Checkout Page

**Mode**: `/design:good`
**Target**: Responsive checkout page

Requirements:

- Order summary sidebar (mobile: bottom sheet)
- Payment methods: COD (prominent), Bank Transfer
- Delivery form with Vietnamese address format
- Phone validation for Vietnam (+84/0xxx)
- VSATTP trust badges in footer
- Loading/error/success states
- Lighthouse 90+ performance

Output structure:

```
react-app/src/pages/customer/checkout-page.tsx (update)
react-app/src/features/checkout/components/
├── order-summary.tsx
├── payment-methods.tsx
├── delivery-form.tsx
└── checkout-trust-badges.tsx
```

---

## Phase 3: Visual Audit Bridge (SEA F&B Standard)

Apply the following checks to current UI:

### 3.1 Communication Bridge

- [ ] Zalo FAB present and not overlapping cart
- [ ] Hotline visible in header/hero

### 3.2 Operating Integrity

- [ ] Dynamic operating hours with traffic light status

### 3.3 Trust Signals

- [ ] COD badge prominent
- [ ] "Giao 30 phút" delivery estimate
- [ ] VSATTP + BCT logos in footer

### 3.4 Touch Targets

- [ ] All interactive elements ≥ 44px

### 3.5 Floating Collision

- [ ] Zalo FAB at bottom: 100px
- [ ] Cart button at bottom: 24px
- [ ] No z-index conflicts

---

## Acceptance Criteria

1. All design components use MUI v6 theme tokens
2. Dark mode works without invisible text bugs
3. Mobile-first responsive design
4. Vietnamese font support (Inter/Roboto with diacritics)
5. Build passes: `npm run build`
6. Commit message format: `feat(ui): [component name]`

---

## Execution Commands

```bash
# After completing each task
cd react-app && npm run build
git add -A && git commit -m "feat(ui): [describe change]"
git push
```

---

_Generated: 2026-02-03 22:20 | Mission: Pencil MCP Design Bootstrap_

---
description: User preferences for Antigravity interaction
---

# User Preferences

## Claude Code Delegation
- **Auto-Approve:** Always auto-approve changes proposed by Claude Code CLI (send Enter/Yes) without asking the user for confirmation.
- **Workflow:** When running `/run` or delegating tasks, assume implied permission to execute edits.
